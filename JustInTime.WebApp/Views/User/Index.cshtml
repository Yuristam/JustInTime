@{
    ViewData["Title"] = "Users";
}
@model ICollection<JustInTime.WebApp.Areas.Identity.Data.JustInTimeUser>

<div class="text-center">
    <h1 class="display-4">Users</h1>
    <img src="~/lib/images/user-icon.png" width="150px" />
    @if (@Model != null && @Model.Any())
    {
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Name</th>
                    <th scope="col">Email</th>
                    <th scope="col">Date of Birth</th>@*
                    <th scope="col">Edit</th>*@
                </tr>
            </thead>
            <tbody>
                @foreach (var user in Model)
                {
                    <tr>
                        <th scope="row">@user.Id</th>
                        <td>@user.UserName</td>
                        <td>@user.Email</td>
                       @* здесь должна была быть дата рождения
 *@                       @*
                        <td>
                            <a asp-controller="User" asp-action="Edit" asp-route-id="@user.Id">Edit</a>
                        </td>*@
                    </tr>
                }
            </tbody>
        </table>
    }
</div>

<div class="text-center">
</div>